      - name: commit
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          if [ -n "$(git status --porcelain)" ]; then
            echo "changes exist, committing and pushing"
            git commit -am "update blog"
            git push
          fi
